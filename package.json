{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "lerna": "^6.6.2"
  }
}
